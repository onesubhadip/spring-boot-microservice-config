spring:
  data:
    mongodb:
      authentication-database: "admin"
      database: "book-service-db"
      username: "root"
      password: "secret"

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    statusPageUrlPath: /actuator/info
    healthCheckUrlPath: /actuator/health